# Raw Session Note

今日はCognitiveOSの発散枝を保存して、あとで取り出す話をした。
素の会話ログをそのまま保存するだけでなく、Snapshot形式に整えて保存できると便利。

- 保存した枝をWorking DBに残す
- あとでlist/showで取り出す
- Human判断後にReference DBへ昇格する
- false closureを避ける

次はnormalize-snapshotで素ログから構造化されたWorking Snapshotを作りたい。

